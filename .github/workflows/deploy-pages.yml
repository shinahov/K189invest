name: Deploy K189invest to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Prepare the short presentation site
        run: |
          mkdir -p _site
          mkdir -p _site/short
          cp web-presentations/index.html web-presentations/.nojekyll _site/
          cp web-presentations/short/deck.js _site/short/
          cp -R web-presentations/assets web-presentations/shared _site/

      - name: Upload Design 4 site
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
